export enum TransactionType {
    buy = 'buy',
    sell = 'sell',
}

export enum TransactionStatus {
    pending = 'pending',
    success = 'succcess',
    rejected = 'rejected',
    failed = 'failed',
}
